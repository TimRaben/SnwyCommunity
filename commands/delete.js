const discord = require("discord.js");

module.exports.run = async (client, message, args) => {


    guild.channel.delete('ticket-overig')
        
    }

module.exports.help = {
    name: "delete"
}